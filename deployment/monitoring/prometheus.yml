global:
  scrape_interval:     15s

  external_labels:
    monitor: 'ics-monitor'

scrape_configs:
- job_name: 'ics-metric'
  scrape_interval: 5s
  metrics_path: '/metrics/'
  static_configs:
  - targets: ['instrument-craft-shop:9443']
  scheme: https
  basic_auth:
    username: admin
    password: adminadmin
  tls_config:
    insecure_skip_verify: true
